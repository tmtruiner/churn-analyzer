# Модуль предсказания оттока клиентов в телеком-сети

Полнофункциональное приложение для предсказания оттока клиентов телеком-оператора с использованием машинного обучения.

## 📋 Описание

Проект реализует комплексное решение для предсказания оттока клиентов (churn prediction) с использованием:
- **Модель**: CatBoost Classifier
- **Backend API**: FastAPI
- **Frontend UI**: Streamlit
- **Данные**: Telco Customer Churn Dataset (Kaggle)

## 🏗️ Структура проекта

```
.
├── data/                      # Данные (CSV файлы)
│   └── WA_Fn-UseC_-Telco-Customer-Churn.csv
├── models/                    # Сохраненные модели и метаданные
│   ├── churn_model.pkl
│   ├── categorical_features.pkl
│   └── feature_order.pkl
├── plots/                     # Графики и визуализации
│   ├── roc_curve.png
│   ├── pr_curve.png
│   ├── confusion_matrix.png
│   └── shap_summary.png
├── src/                       # Исходный код
│   ├── __init__.py
│   ├── data_loader.py        # Загрузка и предобработка данных
│   ├── eda.py                # Exploratory Data Analysis
│   ├── feature_engineering.py # Feature Engineering (SMOTE, новые признаки)
│   ├── hyperparameter_tuning.py # Настройка гиперпараметров (Optuna)
│   ├── metrics.py            # Расширенные метрики
│   ├── shap_analysis.py      # SHAP анализ важности признаков
│   ├── train.py              # Обучение модели
│   └── api.py                # FastAPI сервис
├── tests/                     # Тесты
│   ├── __init__.py
│   ├── test_data_loader.py   # Unit тесты
│   └── test_api.py           # API тесты
├── app.py                     # Streamlit UI
├── train_model.py            # Скрипт обучения модели
├── run_eda.py                # Скрипт EDA анализа
├── run_full_training.py      # Полный пайплайн обучения
├── requirements.txt          # Зависимости
├── Dockerfile                # Docker конфигурация
├── docker-compose.yml        # Docker Compose
└── README.md                 # Документация
```

## ⚡ Быстрый старт

**Самый быстрый способ запустить проект:**

### Windows:
```bash
start.bat
```

### Linux/Mac:
```bash
chmod +x start.sh
./start.sh
```

### Docker (рекомендуется):
```bash
docker-compose up --build
```

📖 **Подробная инструкция:** см. [QUICKSTART.md](QUICKSTART.md)

---

## 🚀 Установка

### 1. Клонирование и установка зависимостей

```bash
# Установка зависимостей
pip install -r requirements.txt
```

### 2. Подготовка данных

Скачайте датасет [Telco Customer Churn](https://www.kaggle.com/datasets/blastchar/telco-customer-churn) с Kaggle и поместите файл `WA_Fn-UseC_-Telco-Customer-Churn.csv` в папку `data/`.

## 📊 Использование

### 1. EDA анализ

```bash
python run_eda.py
```

Создаст:
- Графики распределений признаков
- Матрицу корреляций
- Распределение целевой переменной
- Текстовый отчет `eda_report.txt`

### 2. Обучение модели

**Рекомендуется: Унифицированный скрипт обучения**
```bash
# Базовое обучение (быстрое)
python train_unified.py --type basic

# Улучшенное обучение (рекомендуется для курсовой)
python train_unified.py --type improved

# Обучение с кластеризацией
python train_unified.py --type clustered
```

**Альтернативные варианты:**
```bash
# Базовое обучение (старый способ)
python train_model.py

# Полный пайплайн (EDA + обучение + SHAP)
python run_full_training.py
```

📖 **Подробное руководство:** см. [TRAINING_GUIDE.md](TRAINING_GUIDE.md)

После обучения будут созданы:
- `models/churn_model.pkl` - обученная модель (или `churn_model_improved.pkl`, `churn_model_clustered.pkl`)
- `models/categorical_features.pkl` - список категориальных признаков
- `models/feature_order.pkl` - порядок признаков
- `plots/` - графики метрик (ROC, PR, Confusion Matrix)
- `metrics_report.txt` - отчет по метрикам

### 3. Запуск API

**Рекомендуется (унифицированный API):**
```bash
uvicorn src.api_unified:app --reload
```

**Альтернативные варианты:**
```bash
# Базовый API (для обычной модели)
uvicorn src.api:app --reload

# API с кластеризацией (для модели с кластеризацией)
uvicorn src.api_clustered:app --reload
```

API будет доступен по адресу: `http://localhost:8000`

**Примечание:** Унифицированный API (`api_unified.py`) автоматически определяет тип модели и работает с любым вариантом.

**Endpoints:**
- `GET /` - Информация о сервисе
- `GET /health` - Проверка здоровья сервиса
- `POST /predict` - Предсказание оттока

**Пример запроса:**
```bash
curl -X POST "http://localhost:8000/predict" \
  -H "Content-Type: application/json" \
  -d '{
    "data": [{
      "gender": "Female",
      "SeniorCitizen": 0,
      "tenure": 12,
      "MonthlyCharges": 53.85,
      ...
    }]
  }'
```

### 4. Запуск UI

```bash
streamlit run app.py
```

UI будет доступен по адресу: `http://localhost:8501`

**Возможности UI:**
- 📁 Загрузка CSV файлов с данными клиентов
- 🔮 Предсказание оттока
- 📊 Визуализация результатов
- 📈 Просмотр метрик модели
- 🔍 SHAP анализ важности признаков
- 📥 Экспорт результатов

### 5. Docker

```bash
# Сборка и запуск
docker-compose up --build

# API: http://localhost:8000
# UI: http://localhost:8501
```

## 🧪 Тестирование

```bash
# Unit тесты
pytest tests/test_data_loader.py -v

# API тесты (требует запущенный API)
pytest tests/test_api.py -v
```

## 📈 Метрики модели

Модель оценивается по следующим метрикам:
- **Accuracy** - Точность
- **Precision** - Точность положительных предсказаний
- **Recall** - Полнота
- **F1-score** - Гармоническое среднее Precision и Recall
- **ROC-AUC** - Площадь под ROC-кривой
- **PR-AUC** - Площадь под Precision-Recall кривой

## 🔍 Особенности реализации

### Feature Engineering
- Создание новых признаков на основе существующих
- SMOTE для балансировки классов
- ADASYN для балансировки классов

### Настройка гиперпараметров
- Optuna для автоматической оптимизации
- GridSearchCV (опционально)

### Анализ важности признаков
- SHAP значения для интерпретации модели
- Визуализация важности признаков

## 📝 Требования

- Python 3.10+
- Все зависимости указаны в `requirements.txt`

## 🤝 Вклад

Проект создан в рамках курсовой работы по теме "Модуль предсказания оттока клиентов в телеком-сети".

## 📄 Лицензия

Учебный проект.

## 🔗 Полезные ссылки

- [CatBoost Documentation](https://catboost.ai/)
- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [Streamlit Documentation](https://docs.streamlit.io/)
- [SHAP Documentation](https://shap.readthedocs.io/)
